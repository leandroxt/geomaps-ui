import React, { ReactElement } from 'react';

export default function InterestArea(): ReactElement {
  return (
    <span>Clique no mapa para selecionar um ponto de interesse e selecione o tamanho</span>
  );
}
